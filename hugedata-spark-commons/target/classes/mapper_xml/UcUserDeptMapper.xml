<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.hugedata.spark.commons.storage.mapper.UcUserDeptMapper" >
  <resultMap id="BaseResultMap" type="cn.com.hugedata.spark.commons.storage.entity.UcUserDept" >
    <id column="RECORD_ID" property="recordId" jdbcType="INTEGER" />
    <result column="USER_ID" property="userId" jdbcType="INTEGER" />
    <result column="DEPT_ID" property="deptId" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Map_Where_Clause" >
    <if test="null != recordId" >
      and uud.RECORD_ID = #{recordId}
    </if>
    <if test="null != ne_recordId" >
      and uud.RECORD_ID != #{ne_recordId}
    </if>
    <if test="null != list_recordId" >
      and uud.RECORD_ID in (<foreach collection="list_recordId" item="i" separator=",">#{i}</foreach>)
    </if>
    <if test="null != userId" >
      and uud.USER_ID = #{userId}
    </if>
    <if test="null != ne_userId" >
      and uud.USER_ID != #{ne_userId}
    </if>
    <if test="null != list_userId" >
      and uud.USER_ID in (<foreach collection="list_userId" item="i" separator=",">#{i}</foreach>)
    </if>
    <if test="null != deptId" >
      and uud.DEPT_ID = #{deptId}
    </if>
    <if test="null != ne_deptId" >
      and uud.DEPT_ID != #{ne_deptId}
    </if>
    <if test="null != list_deptId" >
      and uud.DEPT_ID in (<foreach collection="list_deptId" item="i" separator=",">#{i}</foreach>)
    </if>
  </sql>
  <sql id="Order_By_Clause" >
    <if test="null != order" >
      <trim prefix="order by " >
        <foreach collection="order" item="orderItem" separator="," >
          <if test="'recordId' == orderItem.column" >
            uud.RECORD_ID ${orderItem.dir}
          </if>
          <if test="'userId' == orderItem.column" >
            uud.USER_ID ${orderItem.dir}
          </if>
          <if test="'deptId' == orderItem.column" >
            uud.DEPT_ID ${orderItem.dir}
          </if>
        </foreach>
      </trim>
    </if>
  </sql>
  <sql id="Base_Column_List" >
    uud.RECORD_ID, uud.USER_ID, uud.DEPT_ID
  </sql>
  <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select 
    <include refid="Base_Column_List" />
    from uc_user_dept uud
    where uud.RECORD_ID = #{recordId,jdbcType=INTEGER}
  </select>
  <select id="selectByMap" resultMap="BaseResultMap" parameterType="map" >
    select
    <include refid="Base_Column_List" />
    from uc_user_dept uud
    <where >
      <include refid="Map_Where_Clause" />
    </where>
    <include refid="Order_By_Clause" />
  </select>
  <select id="countByMap" resultType="java.lang.Integer" parameterType="map" >
    select count(1)
    from uc_user_dept uud
    <where >
      <include refid="Map_Where_Clause" />
    </where>
  </select>
  <insert id="insertSelective" parameterType="cn.com.hugedata.spark.commons.storage.entity.UcUserDept" >
    <selectKey resultType="java.lang.Integer" keyProperty="recordId" order="AFTER" >
      select @@identity
    </selectKey>
    insert into uc_user_dept
    <trim prefix="(" suffix=")" suffixOverrides="," >
      RECORD_ID,
      <if test="userId != null" >
        USER_ID,
      </if>
      <if test="deptId != null" >
        DEPT_ID,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      #{recordId,jdbcType=INTEGER},
      <if test="userId != null" >
        #{userId,jdbcType=INTEGER},
      </if>
      <if test="deptId != null" >
        #{deptId,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <delete id="deleteById" parameterType="java.lang.Integer" >
    delete from uc_user_dept
    where RECORD_ID = #{recordId,jdbcType=INTEGER}
  </delete>
  <delete id="deleteByIds" >
    delete from uc_user_dept where RECORD_ID in 
    <foreach collection="list" item="id" open="(" separator="," close=")" >
      #{id}
    </foreach>
  </delete>
  <delete id="deleteByMap" >
    delete from uud using (uc_user_dept as uud) 
    <where >
      <include refid="Map_Where_Clause" />
    </where>
  </delete>
  <update id="updateSelectiveById" parameterType="cn.com.hugedata.spark.commons.storage.entity.UcUserDept" >
    update uc_user_dept
    <set >
      <if test="userId != null" >
        USER_ID = #{userId,jdbcType=INTEGER},
      </if>
      <if test="deptId != null" >
        DEPT_ID = #{deptId,jdbcType=INTEGER},
      </if>
    </set>
    where RECORD_ID = #{recordId,jdbcType=INTEGER}
  </update>
  <update id="updateSelectiveByMap" parameterType="map" >
    update uc_user_dept uud
    <set >
      <if test="record.recordId != null" >
        RECORD_ID = #{record.recordId,jdbcType=INTEGER},
      </if>
      <if test="record.userId != null" >
        USER_ID = #{record.userId,jdbcType=INTEGER},
      </if>
      <if test="record.deptId != null" >
        DEPT_ID = #{record.deptId,jdbcType=INTEGER},
      </if>
    </set>
    <where >
      <if test="null != map.recordId" >
        and uud.RECORD_ID = #{map.recordId}
      </if>
      <if test="null != map.list_recordId" >
        and uud.RECORD_ID in (<foreach collection="map.list_recordId" item="i" separator=",">#{i}</foreach>)
      </if>
      <if test="null != map.userId" >
        and uud.USER_ID = #{map.userId}
      </if>
      <if test="null != map.list_userId" >
        and uud.USER_ID in (<foreach collection="map.list_userId" item="i" separator=",">#{i}</foreach>)
      </if>
      <if test="null != map.deptId" >
        and uud.DEPT_ID = #{map.deptId}
      </if>
      <if test="null != map.list_deptId" >
        and uud.DEPT_ID in (<foreach collection="map.list_deptId" item="i" separator=",">#{i}</foreach>)
      </if>
    </where>
  </update>
</mapper>